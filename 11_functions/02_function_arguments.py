def add(a,b,puls=0):  #positional arguments # default argument ,(puls=0)
    """Returns the sum of a and b."""
    return a + b + puls
print (add(2,3,2))
# The function average is called with different sets of arguments, and the results  are printed.
c1 = add(b =3, a=2)  # keyword arguments  # Output: "5" # c1 = add(3,2) # print(c1)
print(c1)
# print(add.__doc__) # Using doc strings methods.
